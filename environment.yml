name: folding-dssp
channels:
  - salilab
  - conda-forge
  - defaults
dependencies:
  - matplotlib
  - python=3.9
  - biopython
  - pandas
  - numpy
  - requests
  - dssp
  - tqdm
  - jupyterlab  # optional but helpful
  - pytest # optional but helpful
  - logomaker # optional but nice for generating sequence logos